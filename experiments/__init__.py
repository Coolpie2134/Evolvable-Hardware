"""
experiments — standalone research code kept out of the main pipeline.

  * experiments.concept — proof-of-concept GA sims that predate the main
    backends (driven by the ui.concept_gui playground). Self-contained; the
    production substrates do not depend on anything here.
"""
